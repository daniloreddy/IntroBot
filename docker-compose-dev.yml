services:
  intro-bot:
    build: .
    container_name: introbot-dev
    restart: "no"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
